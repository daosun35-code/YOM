{
  "mcpServers": {
    "github": {
      "command": "bash",
      "args": [
        "-lc",
        "exec npx -y mcp-remote https://api.githubcopilot.com/mcp/ --header \"Authorization: Bearer $GITHUB_PAT_TOKEN\""
      ]
    }
  }
}
